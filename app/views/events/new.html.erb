<%= form_for setup_event(@event), :url => events_path do |f| %>

    <%= render :partial => 'form' , :locals =>{:f => f} %>
    <br>
    <%= f.submit "Create" %>
<% end %>

<ul class="pager">
  <li class="previous"><%= link_to events_path do%>&larr;Back<% end %></li>
</ul>